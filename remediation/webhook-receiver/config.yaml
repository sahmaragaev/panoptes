server:
  host: 0.0.0.0
  port: 5001

ansible:
  playbook_dir: /app/ansible/playbooks
  inventory: /app/ansible/inventory/hosts.yml

cooldown:
  duration_minutes: 30

logging:
  file: /var/log/remediation.log
  level: INFO
