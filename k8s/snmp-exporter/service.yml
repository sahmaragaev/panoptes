apiVersion: v1
kind: Service
metadata:
  name: snmp-exporter
  namespace: panoptes
  labels:
    app.kubernetes.io/name: snmp-exporter
    app.kubernetes.io/part-of: panoptes
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: snmp-exporter
  ports:
    - port: 9116
      targetPort: 9116
      protocol: TCP
