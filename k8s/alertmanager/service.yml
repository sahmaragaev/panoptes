apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: umas
  labels:
    app.kubernetes.io/name: alertmanager
    app.kubernetes.io/part-of: umas
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: alertmanager
  ports:
    - port: 9093
      targetPort: 9093
      protocol: TCP
      name: http
